# clash-sub-parser 经验

## 订阅表单逻辑（2026-07-14 ~ 2026-07-15）

### 自动命名
- 粘贴 URL 后自动取 hostname **二级标签**（`parts[length-2]`），首字母大写。
  - `z.7li7li.com` → `7li7li`
  - `msub.example.com` → `Example`
  - `www.example.com` → `Example`
- 仅当用户**手动改过名字**时不再覆盖；空名或仍是上次自动名时，URL 变更可更新自动名。
- 曾试过完整 hostname，用户要求改回二级标签。

### 主订阅
- 主订阅只在**列表页卡片**操作（`设为主订阅`），不要塞进添加/编辑弹窗。
- 后端 `is_primary` 仍由 PATCH 设置；表单保存时不要再提交 `is_primary` 切换。

### 启用开关
- `enabled` 默认 true。禁用后：不调度拉取、不进入生成 proxies、主订阅 header 也不取它。
- 开关在列表卡片，不在表单里硬塞。

### 候选节点隔离
- 候选节点 = 当前订阅 `source_nodes`（无则 `raw_nodes`）+ 当前 `manual_nodes`。
- **禁止** fallback 到 `getAllSubscriptionNodes()` / 全局 `allNodes`。
- 新建未拉取前候选为空是正确行为；提示用户先保存并拉取，或加手动节点。

### 功能面板（表单）
- 手动节点 / 初筛正则 / 精修 / 节点重命名：默认关闭，chip 打开后才显示输入区。
- 编辑时若已有对应数据，自动打开对应面板。
- 关闭面板再保存：清空该功能相关配置。
- 编辑已有订阅时可在表单内「拉取节点」。

### 节点重命名（前缀后）
- `node_renames` 是 map：`{加前缀后的名字: 最终名字}`。
- 应用顺序：筛选原始节点 → 加前缀 → 应用 renames → 写入 `raw_nodes`。
- 策略组 regex 匹配的是 **最终名**（raw_nodes 里的 name），不是上游原始名。
- 订阅卡片点「节点」后也能直接改名：预览弹窗里点「改名」保存。
- 预览改名的 key 必须用前缀后基础名，不能用已经改过的最终名，避免映射连环覆盖。

### Dogegg 进不了策略组（2026-07-15，2026-07-20 已清排除词）
- 节点已在 proxies，但地区/自动组全 0 匹配。
- 根因：节点名含「流量站」，而 `自动选择/选择节点/游戏` 等正则排除了 `流量`。
- 不是生成 bug。2026-07-20 已从组正则去掉「流量」排除；也可用 rename 改最终名。

### 7li 节点变少（2026-07-20）
- 现象：UI/DB 只有 ~22–23 个 ss，上游直接拉能看到更多。
- 根因：默认 UA `ClashforWindows/0.20` 被机场裁剪协议；`clash.meta` / `clash-verge/v2` 可拿到完整（含 vless/hysteria2）。
- 处理：默认 UA 改为 `clash.meta`；改完后重建并重新拉取订阅。

### 测速 / 归属国（已移除，2026-07-20）
- 已从后端 API、前端 UI、配置、compose 中彻底删除。
- 原因：真实代理延迟/出口 IP 归属强依赖外部代理引擎（mihomo），与「独立项目」目标冲突。

### 删除/拉取竞态（后端）
- 失败拉取也必须写 `last_fetched_at`，否则调度器每分钟狂重试。
- 拉取过程中订阅被删：先缓存 id，提交前 `get` 再写；`StaleDataError` → 404，不要 500。
- SQLite 用 WAL + busy_timeout。

### 节点组保存
- 保存 payload 只传 `include_entries`；后端从中派生 `regex_rules` / `kind` / `include_*`。
- 正则在统一排序列表行内编辑，不用删了重加。
- 保存失败要 toast + 弹窗内错误文案，不能只 `console.error`。

### 减法原则（2026-07-15）
- 删掉 `node_group_service` 里重复的 `dedup_names/with_fallback/resolve_entries`，统一用 `group_utils`。
- 前端不再双写 `regex_rules`；列表不再额外展示 `regex_rules` 徽章（entries 里已有正则项）。
- 深夜模式补齐规则搜索/功能 chip/重命名/归属国等新 UI。

### 正则是虚拟条目，不是冻结节点
- 节点组 `include_entries` 支持 `regex` 类型：只存正则字符串，生成/预览时动态展开匹配到的节点。
- 不要再提供“冻结为静态节点”按钮；用户要的是筛选语义，不是把当前匹配结果写死。
- 统一排序条目里：node / group / group_nodes / regex 可一起排序，正则可行内编辑。
- 数据迁完后：以 `include_entries.regex` 为唯一真源；`regex_rules` 只是画面/镜像字段，不做长期兼容回退。

### 正则被清空事故（2026-07-15）
- 现象：所有 `regex_rules` 变成 `[]`，地区组匹配全空。
- 根因：虚拟条目改造后，更新 payload 带 `include_entries` 但没有 regex entry 时，回写把 `regex_rules` 清空。
- 恢复：从 `config_snapshots.id=8`（`auto-before-batch-rules`）恢复，并迁移为 `include_entries` 的 `regex` 虚拟条目。
- 后续原则：**只做迁移，不做长期兼容回退**。数据迁完后以 `include_entries.regex` 为准；`regex_rules` 仅镜像展示。
- 以后改节点组结构前先自动快照。

### 节点组兜底
- `add_fallback=true` 时追加的是 **PASS**，不是 REJECT。
- 规则里的广告拦截 REJECT 不要动，那是规则策略，不是策略组兜底。

### 部署注意
- 数据在 docker volume `backend-data`，重建镜像不会丢订阅。
- **正确部署：`docker compose up -d --build`**。热更 `frontend_dist` 只适合临时调试，不当生产手段。
- 做完代码改动后：验证 → commit → 默认 push `dev`。

## 链式绑定世界状态（2026-07-31）

图谱 god-nodes 显示 generate 与 proxy_chain 共用 `resolve_group_members`，`proxy_chain_service` 里台账、应用绑定、环检测、预览曾各扫一遍订阅与策略组。

### 改了什么
1 抽出 `_load_chain_world`，四处共用扫库与 `leaves_only=True` 叶子解析
2 `apply_bindings` 仍用入参 proxies 的名字做 resolve 名表，订阅归属仍从库读
3 补 `resolve_group_members` 单测：导出保留嵌套组名、leaves 展开、regex 排除、互指 group_nodes 不炸

### 验证
`backend` 下 `pytest` 全绿，76 passed（含 test_proxy_chain_v2 与 test_utils）

### 未做
1 未拆 `getApiErrorMessage` 前端枢纽
2 未大拆 subscription_service 拉取管线
3 未重建 docker（本次纯后端逻辑，可按需 up --build）

## 收尾 checklist
1. 自测 / pytest / 前端 build
2. 重建容器并确认订阅数据还在
3. 更新 `.exp.md`（过时说法立刻改）
4. 写 `memory/YYYY-MM-DD.md`
5. `git commit` + `git push origin dev`

## 订阅 raw_nodes 管线（2026-07-31）

`fetch_subscription_nodes` 与 `_refresh_selected_nodes` 共用 `_materialize_raw_nodes`：
合并源 → 筛选 → 前缀 → renames → 去重。顺序与 `.exp` 里 rename 约定一致。
验证：pytest 76 passed。

## 全面优化（2026-08-22）

### 后端修复
1 `update_subscription` 里 `_clear_primary(db)` 被调了两次，删掉一处
2 `create_manual_node_subscription` 的 `fetch_comments=[] if payload.is_primary else []` 恒等式，直接写 `[]`
3 `_refresh_selected_nodes` manual 分支和有源分支代码完全重复，合并为一个条件
4 `proxy_chain_service.update_binding` merged 字典里 target_id/target_name 三元表达式两分支一样（死逻辑），化简为 `data.get(key, item.xxx)`
5 `snapshot_service.create_snapshot` prune 不再全表加载 id，改 `order_by desc + offset(KEEP_COUNT) limit 1` 取 cutoff 再 `delete where id <= cutoff`
6 `settings.py import_config_endpoint` 循环内每行重算 `valid_columns`，提到行循环外
7 `group_utils` 新增 `_compile_pattern` lru_cache，策略组 regex 条目不再每个 group 重复 re.compile

### 前端修复
8 `NodeLedger.vue` saveChain/clearNodeChain：旧绑定逐条 await 改 `Promise.allSettled` 并行；删除失败不再阻断新绑定创建；清节点链加确认弹窗
9 `DnsSettings.vue` applyTemplate 覆盖 DNS 服务器配置前先确认，防误覆盖当前编辑
10 `RuleImportModal.vue` links 模式解析失败静默跳过 → 记数并在结果区显示「N 行无法解析已跳过」，hostname 加合法性校验防 base64 host 乱码进规则
11 `Subscriptions.vue` saveNodeRenames 删掉保存改名后多余的远端 fetchSubscription 重拉（updateSubscription 后端已重算 raw_nodes）

### 死代码清理
12 删 `composables/useFormValidation.ts`（94 行零消费者）
13 删 `urlState.ts` 里无人调用的 `useUrlStates` 批量函数

### 有意不做
- token_auth_middleware 每请求查 security settings：缓存会让 token 变更延迟生效，风险大于收益
- `api/mock.js` 演示模式：有 VITE_DEMO_MODE 开关控制，保留

验证：pytest 101 passed，npm run build + vue-tsc --noEmit 全绿。

## 前端显示逻辑重构（2026-08-22 下午）

用户反馈聚焦两块：列表展示（大数据量卡顿、信息密度）和交互反馈（状态同步、按钮态、弹窗逻辑）。

### 列表展示
14 `NodeLedger.vue` 全量渲染所有节点卡片 → 加分页（50/100/200 每页，筛选变化自动回第 1 页），复用规则页 pager-card 模式
15 `NodeGroups.vue` 模板每张卡反复调 `previewById`/`originalIndex`（O(n) findIndex）→ 预构建 `indexMap` computed，列表渲染从 O(n²) 降 O(n)
16 `Rules.vue` 全局规则搜索每条规则每次按键重拼 haystack → `searchIndex` computed 按 allRules 预计算 Map

### 交互反馈
17 `NodeGroupModal.vue` 打开时 loadSources 无 loading/错误处理，弹窗空白卡住 → 加 sourcesLoading 提示行 + 失败写 error
18 `ProxyChains.vue` toggleEnabled/removeBinding 删除无确认、可连点 → 加 workingId 行级防抖 + store.confirm 删除确认
19 `DnsSettings.vue` 刷新按钮无 disabled/loading 态 → 加 loading ref
20 `ConfigHistory.vue` 展开快照详情 snapData 全局共享有竞态（快速点开另一条旧响应覆盖新数据）→ snapDataSeq 代次守卫丢弃过期响应
21 `RuleCategoryDetail.vue` applyPresets 导入后新规则在列表尾部但停在当前页看不到 → normalizeSortOrder + 跳到最后一页

验证：vue-tsc --noEmit + npm run build 全绿；docker compose build 后容器 healthy（镜像 f0677700597a）。

### 节点列表改紧凑表格（用户反馈：卡片占一大块信息太少）
22 `NodeLedger.vue` 桌面端从大卡片改为紧凑表格（一行一节点：名称/协议/订阅/地址/链路/操作，复用 rules-table 样式），移动端 ≤760px 保留精简卡片（去掉 UDP/TLS/传输等次要字段）。分页保留。

### 整体易用性梳理（用户要求整体优化，不为需求造需求）
23 未保存草稿静默丢失：Rules / RuleCategoryDetail 加 `onBeforeRouteLeave` 确认 + `beforeunload` 拦截
24 NodeGroups 搜索每次按键重拼 resolved_nodes 大字符串 → groupSearchIndex 预计算 Map
25 死路由 `/rules/:id`（RuleDetail.vue 无入口、功能与分类编辑页重复）→ 删文件和路由及 getRule API 封装
26 列表页搜索条件刷新即丢、与规则分类页行为不一致 → Subscriptions/NodeGroups/NodeLedger/ProxyChains 统一接 useUrlState
27 ProxyChains 空状态只有灰字无引导 → 加「新建绑定」按钮

验证：pytest 101 passed；vue-tsc + build 全绿；容器 healthy。
