## 1. Backend execution, inventory, and runtime observability

- [x] 1.1 `executor` — Add the protected `GET /api/probe/status` projection in `backend/app/routers/probe.py` and its single-owner runtime state in `backend/app/services/scheduler.py`; freeze the response fields from `design.md`, sanitize all failure output to a stable error code, and verify with focused async API tests for disabled, initializing, waiting, running, failed, server time, and null next time.
- [x] 1.2 `executor` — Refactor only the scheduler inventory call so periodic jobs probe all `raw_nodes` of enabled subscriptions without subscription capability filtering; preserve deduplication and the existing per-process overlap guard, and verify with an isolated database test containing a node excluded from export filtering plus a concurrent tick test.
- [x] 1.3 `executor` — Extend the probe platform catalogue and dispatch path for Claude `region_signal` observations and ChatGPT `web` / `app` subobservations with the exact `observation_kind`, `tier`, and `subobservations` contract in `design.md`; preserve `trust_env=False`, shared node session, media deadline, evidence allowlist, and legacy result semantics, then verify with offline httpx fixtures for confirmed, partial, challenge, rate-limit, timeout, malformed and secret-redaction cases.
- [x] 1.4 `executor` — Update backend summary/detail serialization and capability tests so `claude` is visible as an observation but never satisfies full-unlock predicates, and ChatGPT tiered outcomes only satisfy media filtering when the frozen top-level full condition is met; verify `pytest` tests for `capability_filter`, probe persistence, catalogue, and API contracts pass.

## 2. Frontend bounded sliding pool and scheduler awareness

- [x] 2.1 `executor` — Extend `frontend/src/api/index.ts` and `frontend/src/api/types.ts` with typed probe-status retrieval and optional `AbortSignal` support for `probeNode`; retain existing API paths and CSRF/auth behavior, then verify TypeScript typecheck passes.
- [x] 2.2 `executor` — Replace `NodeLedger.vue` fixed `CHUNK_SIZE` batch loop with the finite worker-pool contract from `design.md`: fetch one immutable settings snapshot per manual run, start no more than `probe_concurrency` singleton requests, dispatch the next node immediately on completion, merge each returned result immediately, and preserve accurate success/failure/progress counts; verify with a deterministic extracted pure helper or Node test proving a slow request does not block a freed slot and active count never exceeds the snapshot limit.
- [x] 2.3 `executor` — Implement cancellation semantics in `NodeLedger.vue` with a dispatch-stop flag plus active request abort controllers; preserve completed results and state clearly that only subsequent browser dispatch is stopped, then verify a synthetic test for stop-before-dispatch, stop-with-in-flight requests, cleanup, and no post-stop new request.
- [x] 2.4 `executor` — Add the compact NodeLedger scheduler-status banner and server-time-calibrated countdown without copying Settings configuration inputs; refresh status at a bounded interval, clean timers on unmount, display disabled, initializing, waiting, running and failed states accessibly, and verify with DOM/unit tests and `npm run typecheck` plus `npm test`.
- [x] 2.5 `executor` — Extend centralized `nodeLedgerDomain.ts` presentation and platform definitions for Claude and ChatGPT tier labels while keeping Claude non-success and only confirmed highest ChatGPT tier as success; verify existing evidence presentation tests and new matrix tests cover legacy rows, partial, challenge, rate-limit, timeout and unrecognized payloads.

## 3. Integration verification and review gates

- [x] 3.1 `executor` — Run the scoped backend and frontend harnesses: `pytest -q backend/tests/test_probe_orchestration_contract.py backend/tests/test_probe_orchestration_execution.py backend/tests/test_probe_provider_catalogue.py backend/tests/test_probe_persistence_and_api.py`, `npm run typecheck`, and `npm test` from `frontend`; record actual exit codes and failures without modifying deployment configuration.
- [x] 3.2 `reviewcommon` — Independently inspect the implementation against this OpenSpec and rerun the scoped harnesses; reject any deviation from client concurrency ceiling, all-node scheduler inventory, truthful cancellation, single-instance scheduler claims, AI tier semantics, isolation, evidence redaction, or changed existing media capability behavior. Deliver `APPROVED`, `REJECTED`, or `REPLAN_REQUIRED` with file-and-line evidence.
- [x] 3.3 `critic` — Conduct adversarial end-to-end contract review: slow node with free slot, multi-tab manual runs, frontend abort versus server persistence, process restart before the first tick, disabled cron, active run overlap, no enabled nodes, filtered export node still periodically probed, Claude trace success without product success, ChatGPT Web/App disagreement, and hostile evidence strings. Verify no secret, endpoint credential, raw body, or proxy URL reaches output; issue `CRITIC_VERDICT: PASSED` or `CRITIC_VERDICT: REJECTED`.

## 4. Explicitly gated IP Risk work — do not dispatch before confirmation

- [x] 4.1 `planner` — Obtain and record Einck’s explicit decision to use an authorized Scamalytics API, disclose verified exit IPs to that third party, configure the API key as a deployment secret, and define cost/rate allowance. Do not create or dispatch implementation work without this evidence.
- [x] 4.2 `executor` — Only after task 4.1 is satisfied, implement the default-disabled `_ip_risk` observation exactly as `ip-risk-observation` specifies: identity-consensus prerequisite, official API only, strict timeout/error taxonomy, secret-free persistence/details, and zero influence on health, media filtering, compilation, export and naming. Verify fully offline API fixtures, serialization/redaction tests, and a regression test proving identical export/filter results with low/high risk scores.
- [x] 4.3 `reviewcommon` and `critic` — Only after task 4.2, independently verify authorized-only egress, no public HTML scraping fallback, no secret leakage, no unbounded retry, API-disabled behavior, and risk-score isolation before approval.
