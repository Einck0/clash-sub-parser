## 1. Frontend facet contract and compact controls

- [ ] 1.1 Replace the single-value protocol, status and country fields with the frozen `FacetFilterState` arrays in `frontend/src/views/nodeLedgerDomain.ts`; implement pure normalization, OR-within-facet and AND-across-facet matching without changing media verified-full AND semantics, and verify `frontend/tests/node_ledger_domain.test.mjs` covers empty, one, many, mixed-facet, legacy media and reset cases.
- [ ] 1.2 Update `NodeLedger.vue` to construct, reset and receive only canonical facet state; preserve metrics, virtual-table selection, probe targeting, sorting and existing API calls, and verify `npm test -- node_ledger_domain.test.mjs` passes without unused legacy field references.
- [ ] 1.3 Replace persistent country and media Chip rows in `LedgerSearchFilter.vue` with compact accessible facet controls plus bounded active-filter summary; keyboard Enter Space Escape, click-outside close, focus visibility, labels and aria-expanded are required, and verify component tests assert no candidate option is mounted while its facet is closed.
- [ ] 1.4 Add or extend frontend browser/component coverage for 375x812, 768x900, 1024x900 and 1440x900: closed facets preserve workspace, selected counts and removal work, keyboard behavior works, 375px has no horizontal document overflow, and mobile interactive controls meet the established 44px target requirement.
- [ ] 1.5 Run `npm test`, `npm run typecheck`, `npm run build` and the relevant Playwright or DOM geometry suite from `frontend/`; attach exact exit-code evidence and list every touched frontend file.

## 2. Backend shared-session and Netflix fast-path implementation

- [x] 2.1 Add failing backend tests proving that one healthy node with multiple enabled media platforms constructs exactly one `httpx.AsyncClient` with the candidate loopback proxy and `trust_env=False`, preserves sibling outcomes, and never opens a media session after handshake failure; then implement the shared per-node dispatcher session in `providers.py` and verify the focused tests pass.
- [x] 2.2 Thread the resolved `media_timeout_s` through `probe_single_node()` into the media dispatcher; apply a single monotonic media-stage deadline without exceeding remaining node budget, and verify tests cover configured deadline usage, no request after deadline, timeout normalization, sibling preservation and unchanged transport liveness authority.
- [x] 2.3 Extend the catalogue request path to accept a caller deadline without loosening its one-transient-error retry ceiling; verify definitive restriction, challenge, 429 and contract-drift outcomes still make zero retries and that timeout cannot leak a direct host request.
- [x] 2.4 Implement fixture-driven Netflix fast conclusion: only a valid two-letter country returns verified full and only HTTP 403 returns existing blocked classification; invalid JSON, empty/malformed data, non-conclusive status, transport failure and exhausted remaining budget must follow the specified fallback or structured non-success result. Verify each branch with synthetic `httpx` responses and no raw body or endpoint query is persisted.
- [x] 2.5 Before enabling the Fast.com call in a real probe, run a controlled existing-node smoke test through the isolated loopback runner with secrets redacted and capture only status, final host, elapsed time and classifier output. If the endpoint requires an opaque token, cookie or unsupported contract, leave fast conclusion disabled, retain title fallback and submit a StrategyConsultation rather than adding a compatibility secret.
- [x] 2.6 Run focused `pytest` suites for provider catalogue, security deadlines and orchestration; then run the applicable backend regression suite. Record exact commands, results and changed backend files; do not change schema, router, ORM, migration or settings API files without an approved plan revision.

## 3. Combined acceptance and independent gates

- [ ] 3.1 Integrate frontend and backend changes without modifying public probe request shapes, result taxonomy, database schema, runner lifecycle or node concurrency cap; run `git diff --check`, focused frontend tests, focused backend tests, frontend typecheck and production build, and document command output.
- [ ] 3.2 Reviewer independently compare the final diff to `proposal.md`, all delta specs and `design.md`; rerun backend shared-session deadline fallback tests plus frontend facet and geometry tests; reject any direct-egress path, secret exposure, loss of historical full-unlock compatibility, false Netflix conclusion or persistent flat Chip candidate list.
- [ ] 3.3 Critic perform adversarial quality review of closed/open facets, all-timeout and partial results, cancellation behavior, no-subscription inventory, narrow mobile keyboard/focus behavior, notification silence and rollback assumptions; return `CRITIC_VERDICT: PASSED` only with reproduced evidence, otherwise give actionable rejection.
- [ ] 3.4 Planner alone decides production release after Reviewer APPROVED and CRITIC_VERDICT PASSED. Release verification must confirm service health and a non-secret real probe path; rollback is prior application image only, with no database downgrade or probe-history deletion.
