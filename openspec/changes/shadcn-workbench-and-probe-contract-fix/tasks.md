## 1. Freeze contracts, harnesses, ownership, and red gates

- [ ] 1.1 [Executor backend-identity] Add fixture-only failing tests for canonical `name|type|server:port` identity, ledger `node_key`, identity-bearing privacy-safe summary values, duplicate-name isolation, status preservation, successful-set convergence, failed-refresh retention, and late-write summary exclusion; own only `backend/tests/test_probe_identity_contract.py`, `backend/tests/test_proxy_chain_v2.py`, and relevant existing focused probe tests. Verify each new assertion fails against the pre-change implementation and run `PYTHONPATH=. pytest -q` on the focused modules with no live database or outbound request.
- [ ] 1.2 [Executor frontend-contract-tests] Add fixture-backed failing Node Ledger domain and Chromium cases for summary key/value equality, all status labels, direct-name-lookup prohibition, duplicate-name chain-action guard, progressive page merge, 375/768/1024/1440 no-overflow, and keyboard overlay lifecycle; own only `frontend/tests/node_ledger_domain.test.mjs`, `frontend/tests/node_ledger_visual_browser.test.mjs`, plus a new dedicated test file if needed. Mock every `/api/**` response and verify the new tests fail before implementation without mutating production data.
- [ ] 1.3 [Planner dispatch constraint] Freeze file ownership before writing: `backend/app/services/node_identity.py`, `backend/app/services/probe/service.py`, and subscription lifecycle paths belong only to backend-identity; `frontend/package.json`, lockfile, `components.json`, `frontend/src/lib/**`, `frontend/src/assets/theme.css`, and `frontend/src/components/ui/**` belong only to frontend-foundation; `frontend/src/views/nodeLedgerDomain.ts`, `NodeLedger.vue`, and `components/ledger/**` belong only to ledger-integration. Treat `frontend/src/style.css` as a hotspot: assign one serialized owner per merge window and record the owner in the dispatch card. Verify no two active executor cards have overlapping write scope.

## 2. Backend canonical identity and convergent persistence

- [ ] 2.1 [Executor backend-identity; depends 1.1] Implement and use the one pure backend canonical-node-key constructor across probe persistence, cache, ledger projection, scheduler inventory, and current-set calculation; extend the ledger response schema so each row has a required key, and make summary map values contain only the frozen identity pair plus allowed summary fields. Verify focused pytest proves map-key/value equality, no forbidden summary fields, exact duplicate-name separation, 51,200-byte envelope limit, cursor continuation, and current API route compatibility.
- [ ] 2.2 [Executor backend-identity; depends 2.1] Implement idempotent current-set probe/cache convergence after successful remote refresh, manual-node recomputation, selection-affecting subscription updates, and subscription deletion; add the summary read fence that excludes keys absent from the current enabled final-node set. Verify focused tests prove historical/renamed placeholder pruning, failed-refresh preservation, removed-node late-write invisibility, no deletion of current records, and no live-data access.
- [ ] 2.3 [Executor backend-identity; depends 2.2] Re-run the affected regression suite `PYTHONPATH=. pytest -q tests/test_probe_persistence_and_api.py tests/test_probe_routes.py tests/test_probe_orchestration_contract.py tests/test_probe_orchestration_execution.py tests/test_probe_capability_predicate.py tests/test_proxy_chain_v2.py tests/test_subscription_service.py` (adjust only for real test-file names) and `ruff check app tests`; retain actual output and do not weaken tests or replace the current database with a fixture database.

## 3. Install the inspectable shadcn-vue foundation

- [ ] 3.1 [Executor frontend-foundation] Resolve current compatible shadcn-vue/Reka UI v2 package versions from the official distribution/lock resolution, add only needed dependencies and a Vite-compatible `components.json`, then create the local `cn` utility and source-owned primitive directory convention. Own only `frontend/package.json`, `frontend/package-lock.json`, `frontend/components.json`, `frontend/vite.config.js`, `frontend/src/lib/**`, and initial primitive source. Verify `npm ci`, `npm run typecheck`, and a dependency/source audit prove no runtime CDN, no deprecated `radix-vue` package, no unpinned added runtime dependency, and local primitive source is present.
- [ ] 3.2 [Executor frontend-foundation; depends 3.1] Establish the semantic Void/panel/card token system and source-owned shadcn-style Button, Badge, Input, Select, Checkbox, Tooltip, DropdownMenu, Dialog/AlertDialog, Sheet/Drawer, and Tabs adapters while preserving existing caller prop/emit contracts through a temporary adapter surface. Own `frontend/src/assets/theme.css` and `frontend/src/components/ui/**`; do not edit Node Ledger or feature views. Verify primitive/unit tests, keyboard focus/Escape/scroll-lock behavior, reduced-motion behavior, and `npm run audit:visual:strict` pass; allow backdrop blur only on the single shared overlay backdrop.
- [ ] 3.3 [Executor frontend-foundation; depends 3.2] Migrate AppModal, BaseDrawer, ConfirmDialog, Toast and IconButton onto the shared primitives without changing modal widths, drawer widths, mobile bottom-sheet behavior, drag close threshold, caller events, or focus semantics. Verify their existing component and real-DOM geometry tests at 375px and 1024px pass, including focus restore and Escape; report every remaining old overlay consumer for later batches.

## 4. Migrate Node Ledger to the frozen identity and component contract

- [ ] 4.1 [Executor ledger-integration; depends 2.1, 3.2, 1.2] Replace Node Ledger state, pagination merge, row keys, selection, live progress, and probe lookup with exact `node_key` ownership; remove all direct name-indexed lookup from templates and domain callers. Permit a name fallback only in one dedicated live-response helper after uniqueness is proved from current rows. Verify domain tests demonstrate reload persistence for `ok`, `fail`, `timeout`, `skipped`, and `unknown`, and duplicate display names never cross-associate records.
- [ ] 4.2 [Executor ledger-integration; depends 4.1] Route desktop table, grid cards, mobile list, filters, metrics, batch targeting, drawer selection, and detail loading through one semantic probe-presentation adapter and shared shadcn primitives. Distinguish untested, unknown, failed, timeout, skipped, and healthy states with explicit text; preserve 36px desktop rows, 44px mobile targets, virtual scrolling, non-destructive probe actions, and exact-key detail fetches. Verify targeted frontend tests, `npm run typecheck`, and fixture-backed browser flows at 375/768/1024/1440 pass with no document overflow.
- [ ] 4.3 [Executor ledger-integration; depends 4.1] Disable only name-addressed chain mutations for a duplicate display-name set and provide the defined unambiguous-state explanation; preserve all safe read, inspect, probe, filtering, and selection paths. Verify duplicate-name fixtures prove no chain mutation request is issued while unique-name fixtures retain their existing chain request/response behavior.
- [ ] 4.4 [Executor ledger-integration; depends 2.2, 4.2] Verify the front-end summary lifecycle against current-set convergence fixtures: removed records never reach current rows or pagination, failed refresh keeps existing state, and an injected late removed-node summary record is ignored. Verify no client-side shadow ledger, dual map, or recreated node-key formatter exists using a static source test plus the browser fixture suite.

## 5. Complete the workbench component migration without hot-file overlap

- [ ] 5.1 [Executor workbench-shell; depends 3.3] Migrate `frontend/src/App.vue` and `frontend/src/components/workbench/**` to the shared token/Sheet/Tooltip/DropdownMenu primitives; preserve router behavior, theme controls, navigation labels, mobile menu, and Quick Export behavior. Do not edit `style.css` unless Planner grants the serialized hotspot lease. Verify shell/navigation/browser tests and all overlay geometry checks pass at declared viewports.
- [ ] 5.2 [Executor management-forms-a; depends 3.2] Migrate only `frontend/src/views/Subscriptions.vue`, `frontend/src/components/SubscriptionForm.vue`, `ManualNodeEditor.vue`, `NodePreviewList.vue`, `frontend/src/views/NodeGroups.vue`, and `NodeGroupModal.vue` to shared form, Dialog, Select, Badge, Tooltip, and Button primitives. Preserve validation, model/emit, source refresh, manual-node, selection, and save behavior. Verify focused component tests, typecheck, and fixture browser smoke checks; do not touch Shell, Ledger, Settings, Rules, or style.css without lease.
- [ ] 5.3 [Executor management-forms-b; depends 3.2] Migrate only `frontend/src/views/Rules.vue`, `RuleCategoryDetail.vue`, `DnsSettings.vue`, `Generate.vue`, `Settings.vue`, `ConfigHistory.vue`, `RuleImportModal.vue`, `RulePresetsModal.vue`, and `QuickExportModal.vue`. Preserve all route/API request shapes, protected-export behavior, form validation, and confirmation semantics. Verify focused tests, typecheck, and fixture browser smoke checks; do not overlap task 5.2 or edit style.css without lease.
- [ ] 5.4 [Executor visual-cleanup; depends 5.1, 5.2, 5.3, serialized hotspot lease] Remove only migrated legacy visual declarations from `frontend/src/style.css`, leaving unrelated behavior untouched; eliminate raw fallback palettes/parallel component tokens from migrated paths and consolidate into `theme.css`. Verify `npm run audit:visual:strict`, a static consumer inventory, and screenshots/DOM checks show no ungoverned backdrop blur, raw migrated palette, global button override, width transition, or accessibility regression.

## 6. Integration, independent review, Critic gate, and release decision

- [ ] 6.1 [Executor integration; depends 2.3, 3.3, 4.4, 5.4] Run `npm test`, `npm run typecheck`, `npm run audit:visual:strict`, `npm run build`, and `npm run test:e2e` in `frontend`; re-run backend focused suite and lint. Capture actual command output, fixture-only Chromium evidence at 375/768/1024/1440, and `openspec validate shadcn-workbench-and-probe-contract-fix --strict`. Verify every command exits zero without service restart, live subscription fetch, live node probe, or production database write.
- [ ] 6.2 [Reviewer reviewcommon; depends 6.1; skills: code-auditor, backend-api-audit, github-code-review] Independently inspect the exact diff against this OpenSpec, re-run declared backend/frontend checks, and adversarially test privacy of summary identity fields, duplicate-name safety, failed-refresh retention, late-write exclusion, no direct-name lookup, dependency provenance, focus/keyboard geometry, absolute-path prohibition, and file-scope compliance. Return only `verdict: APPROVED`, `REJECTED`, or `REPLAN_REQUIRED` with reproducible evidence; do not modify application code.
- [ ] 6.3 [Critic critic; depends 6.1; skills: dogfood, ui-ux-pro-max] Independently use fixture-backed Chromium to challenge all viewports, table/card/mobile ledger states, status readability, zero-subscription and partial-load paths, overlay focus/escape/scroll behavior, notification/operation feedback, reduced motion, and dark-surface hierarchy. Include non-UI regression checks for no current-set summary starvation and no runtime hidden compatibility ledger. Return `CRITIC_VERDICT: PASSED` or `CRITIC_VERDICT: REJECTED` with evidence; do not modify application code.
- [ ] 6.4 [Planner release gate; depends 6.2, 6.3] Before any production deployment, present the irreversible live orphan-probe deletion consequence, verified backup requirement, exact migration validation evidence, and source rollback plan to the authorized human. Do not deploy, restart, refresh real subscriptions, trigger real probes, or enable cleanup against live data without explicit human approval.

## Dispatch DAG and atomic task proposals

| Proposal ID | Assignee | Depends on | Write scope | Required skills | Acceptance harness |
| --- | --- | --- | --- | --- | --- |
| CSP-01 | executor | none | backend identity/service/router/schema/tests only | test-driven-development, build-it-right | tasks 1.1, 2.1–2.3 |
| CSP-02 | executor | none | frontend tests only | test-driven-development, ui-ux-pro-max | task 1.2 |
| CSP-03 | executor | none | frontend package/config/lib/ui/theme only | ui-ux-pro-max, build-it-right | tasks 3.1–3.3 |
| CSP-04 | executor | CSP-01, CSP-02, CSP-03 | Node Ledger domain/view/components only | test-driven-development, ui-ux-pro-max | tasks 4.1–4.4 |
| CSP-05 | executor | CSP-03 | App/workbench shell only | ui-ux-pro-max | task 5.1 |
| CSP-06 | executor | CSP-03 | subscription and node-group management files only | ui-ux-pro-max, test-driven-development | task 5.2 |
| CSP-07 | executor | CSP-03 | rules, DNS, generate, settings, history, export modal files only | ui-ux-pro-max, test-driven-development | task 5.3 |
| CSP-08 | executor | CSP-05, CSP-06, CSP-07 | `frontend/src/style.css` only | ui-ux-pro-max, build-it-right | task 5.4 |
| CSP-09 | executor | CSP-01, CSP-04, CSP-08 | tests/validation evidence only | build-it-right | task 6.1 |
| CSP-10 | reviewcommon | CSP-09 | read-only exact diff and verification | code-auditor, backend-api-audit, github-code-review | task 6.2 |
| CSP-11 | critic | CSP-09 | read-only fixture browser QA | dogfood, ui-ux-pro-max | task 6.3 |
| CSP-12 | planner | CSP-10, CSP-11 | governance/release decision only | strategy-review-orchestration | task 6.4 |

Hot-file policy: CSP-03 alone owns `theme.css` and `components/ui/**` until it completes. CSP-08 is the sole `style.css` editor. CSP-04 alone owns all Node Ledger paths. Any discovered need to touch another active card's file is a stop-and-comment escalation to Planner, not an opportunistic patch.

## 7. Strategy revision 2 — close downstream node-key aliases

- [ ] 7.1 [Executor CSP-13] Replace the dual-key internal probe map with a canonical-key-only map and a shared key lookup for node dictionaries. Update capability filtering, subscription exports, aggregate generation, and group-preview qualification so two nodes with the same display name cannot borrow each other's speed or media result. Own only the backend consumer files named in design revision 2 and focused synthetic duplicate-name tests. Do not change probe transport, persistent schema, public detail projection, or name-addressed chain mutations.
- [ ] 7.2 [Executor CSP-14] Make Node Ledger transport normalization and domain ownership exact-key-only for map, envelope, and array inputs. Require `LedgerNodeItem.node_key`; reject missing/mismatched records; retain a name fallback only for a uniquely proven immediate single-probe response, never for persisted/paged maps. Own only Node Ledger domain/view/components and focused fixtures/tests.
- [ ] 7.3 [Reviewer CSP-15; depends 7.1, 7.2] Independently re-run the identity, persistence/API, generation/capability, group-preview, subscription, route, proxy-chain, Node Ledger domain/contract, typecheck, build, visual audit, and OpenSpec checks. Add adversarial same-display-name, opposite-capability fixtures. Reject any remaining display-name alias or locally reconstructed canonical key.
- [ ] 7.4 [Executor/release environment; depends 7.2] Provision the pinned Playwright Chromium executable and re-run fixture-only browser gates at 375/768/1024/1440. No live service, probe, subscription refresh, production database write, or deploy is permitted. Record the actual browser version and test output; until then browser geometry/visual acceptance remains blocked by environment evidence.

### Revision 2 dispatch additions

| Proposal ID | Assignee | Depends on | Write scope | Required skills | Acceptance harness |
| --- | --- | --- | --- | --- | --- |
| CSP-13 | executor | CSP-01 | key-closed backend probe consumers and their focused tests | test-driven-development, build-it-right | task 7.1 |
| CSP-14 | executor | CSP-04 | Node Ledger domain/view/components and focused tests | test-driven-development, ui-ux-pro-max | task 7.2 |
| CSP-15 | reviewcommon | CSP-13, CSP-14 | read-only exact diff and verification | code-auditor, backend-api-audit | task 7.3 |
| CSP-16 | executor | CSP-14 | test-runtime browser provisioning only | build-it-right | task 7.4 |

Revision 2 hot-file policy: CSP-13 exclusively owns the listed backend key-consumer paths. CSP-14 exclusively owns the Node Ledger paths. CSP-16 must not edit application source. CSP-15 is read-only. Planner must preserve those scopes and avoid merging shared working-directory edits concurrently.
